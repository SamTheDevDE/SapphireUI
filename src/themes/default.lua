--- Default Theme
local theme = {
    background = colors.black,
    foreground = colors.white,
    primary = colors.blue,
    secondary = colors.gray,
    accent = colors.lightBlue,
    error = colors.red,

    Button = {
        background = colors.gray,
        foreground = colors.white,
        focused_background = colors.lightBlue,
        focused_foreground = colors.black,
    },

    Label = {
        background = colors.black,
        foreground = colors.white,
    },
    
    Frame = {
        background = colors.black,
        foreground = colors.white,
    }
}

return theme
